const SearchError = () => {
  const handleClick = () => {
    // TODO: search again
    return
  }
  
  return (
    <div>
      <div>
        Something went wrong..
      </div>

      <button onClick={handleClick}>Try Again</button>
    </div>
  )
}

export default SearchError